export const ROUTE = {
  home: '/',
  stops: 'stops',
} as const;

export const PLACEHOLDER = {
  bus_stop: 'Please select the bus stop first',
  bus_line: 'Please select the bus line first',
} as const;

export const ORDER = {
  asc: 'asc',
  desc: 'desc',
} as const;
